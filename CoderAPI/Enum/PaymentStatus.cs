namespace CoderAPI.Enum
{
    public enum PaymentStatus
    {
        Pending,
        Completed,
        Failed,
        Hold
    }
    public enum CourseStatus
    {
        NotStarted,
        Purchased,
        OnGoing,
        Completed
    }
}
